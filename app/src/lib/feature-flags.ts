/**
 * Feature flags — flip these to enable/disable features site-wide.
 * No env vars needed — just change the value and deploy.
 */

/** When false, the video overlay and "Watch Demo" nav button are hidden. */
export const OVERLAY_ENABLED = true
