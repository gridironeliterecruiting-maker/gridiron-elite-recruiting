// Archives data is now returned by the inbox API as `archivedThreads`.
// This file is kept as a placeholder — the frontend reads archives
// from the same /api/email/inbox response to avoid duplicate Zoho calls.
export {}
